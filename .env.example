# MINEGUARD AI — Workspace Environment Configuration

# Separate FastAPI ML Service URL
ML_API_URL=http://localhost:8000

# Backend ingestion server port & host
HOST=0.0.0.0
PORT=8000

# Frontend Vite Backend API target (if running dashboard on port 3000)
VITE_BACKEND_URL=http://localhost:8000

# Optional API key for ESP32 hardware authentication
MINEGUARD_API_KEY=
